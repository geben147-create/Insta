# 작업기록

## 2026-09-24 20:28 ~ 21:18 (50분) — 인스타 레퍼런스 7편 장면 분해 HTML
- 한 일: 7편 다운로드(yt-dlp) → 컷 검출(ffmpeg scene) → 0.33~1초 간격 프레임 캡처 → 로컬 음성 인식(faster-whisper small, CPU) + BPM/음량 분석 → 영상 1편당 1페이지 HTML 생성
- 결과물: out/index.html, out/p1~p7_*.html, out/assets/, out/img/
- 생성기: work/gen/build.py, sections.py, editkit.py, common.py, v1~v7.py (데이터 수정 후 `python build.py` 재실행)
- 검증: 7페이지+목차 이미지 깨짐 0, 동물 슬롯 치환 정상, 375px 폭 가로 넘침 없음 /
  FFmpeg 스크립트를 가짜 클립으로 실제 실행: v1 52.40초(원본 52.41), v6 29.02초(원본 29.02), 자막·암전 렌더 확인 /
  HyperFrames lint v1·v6 0 errors 0 warnings
- 실패·주의: watch 스킬 ffmpeg -vsync 옵션 오류 → 자체 ffmpeg로 대체 / OpenAI Whisper 키 401(무효) → 로컬 faster-whisper 사용 /
  CUDA cublas64_12.dll 없음 → CPU 사용 / 포트 8765는 다른 프로그램 사용 중 → 8791 사용 / 이 ffmpeg는 fontconfig 없음 → 자막은 fonts/ 폴더 필요
- 저작권: 원본 대사·가사는 옮기지 않고 같은 흐름의 새 대사로 작성
- 다음 행동: 원하는 동물로 1편 골라 첫 프레임 이미지 생성부터 시작
